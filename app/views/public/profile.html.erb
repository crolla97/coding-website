<div class="row">
  <div class="col-sm-8">
    <%= render Posts::ListComponent.new(posts: @posts) %>
  </div>
  <div class="col-sm-4">
    <p class="h4"><%= @profile.full_name %></p>
    <p><%= @profile.username %></p>

    <p>Cake day:<%= @profile.created_at.strftime("%d/%m/%y") %></p>
  </div>
</div>